<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="barBlackList"
            type="com.example.bloqueador_telemarketing.ui.fragment.ActionBarFragment" />

        <variable
            name="barAddBlackList"
            type="com.example.bloqueador_telemarketing.ui.fragment.ActionBarFragment" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="55sp"
        android:background="@color/start_gradient"
        tools:context=".ui.fragment.ActionBarFragment">

        <ImageView
            android:id="@+id/ivBlackList"
            android:layout_width="25sp"
            android:layout_height="0dp"
            android:layout_marginStart="8dp"
            android:onClick="@{barBlackList::blackList}"
            android:src="@drawable/menu"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:id="@+id/imageView4"
            android:layout_width="25sp"
            android:layout_height="25sp"
            android:layout_marginEnd="16dp"
            android:onClick="@{barBlackList::addBlackList}"
            android:src="@drawable/reply"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tvNameApp"
            style="@style/ActionBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="@string/app_name"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@+id/ivBlackList"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>

